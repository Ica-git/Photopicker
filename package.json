{
  "name": "kp-proxy",
  "version": "1.0.0",
  "description": "Proxy server for KupujemProdajem Figma plugin",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "restart": "PowerShell -Command \"netstat -ano | Select-String ':300[01].*LISTENING' | ForEach-Object { $p=($_ -split '\\s+')[-1]; if($p -match '^\\d+$'){taskkill /PID $p /F} }\" & node server.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "sharp": "^0.34.5"
  }
}
